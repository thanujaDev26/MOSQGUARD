import 'package:flutter/material.dart';

import 'AboutUs_Body.dart';

class Aboutus extends StatelessWidget {
  const Aboutus({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: AboutUsPage(),
    );
  }
}
